# Basis-URL der isd-API. Für lokale Entwicklung gegen die lokale isd-Instanz.
VITE_API_BASE_URL=https://isd.local/api/v1

# Exakte Origin desselben API-Ziels für die produktive Content Security Policy.
# Nur Schema + Host + optionaler Port, ohne Pfad und ohne abschließenden Slash.
PORTAL_API_ORIGIN=https://isd.local
